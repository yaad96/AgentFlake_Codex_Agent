# Python dependencies for AgentFlake Codex Agent.
#
# Codex itself is run through the Codex CLI inside Docker. The host
# Python process only needs the Java patch-application parser used by
# AF_Codex_Agent/LLM Scripts/apply_fix.py.

tree-sitter>=0.22
tree-sitter-java>=0.23
